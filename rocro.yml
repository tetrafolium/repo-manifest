inspecode:
  tasks:
    - name: code-analysis
      git-submodules:
        set-up:
          - git submodule update --init --recursive
        sharing: true
      commands:
        main:
          - echo "submodules"
