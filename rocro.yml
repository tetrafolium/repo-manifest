inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: true
          branch: deps~main
      commands:
        - cat ./report.sarif
      reports:
        - path: ./report.sarif
