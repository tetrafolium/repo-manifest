inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          without:
            - some/pattern
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
