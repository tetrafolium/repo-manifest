inspecode:
  tasks:
    - name: git-repo-task
      type: shell
      git-repo:
        manifest: ./default.xml
        set-up:
          - repo init -u ./ -b git-repo-task-configuration-tag -m ./default.xml
          - repo sync
        sharing: true
      reports:
        - path: ./report.json
