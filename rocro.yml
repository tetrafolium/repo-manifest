inspecode:
  tasks:
    - name: code-analysis
      type: shell
      git-submodules:
        set-up:
          - git submodule update --init --recursive
        sharing:
          without:
            - nanopb
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
