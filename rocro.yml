inspecode:
  tools:
    coverity:
      options:
        --commands:
        - export REPODIR=$(pwd)
        - echo $REPODIR
        - ls -alt $REPODIR
        - cd $REPODIR/ant
        - cov-build --dir ~/cov-im --parallel-translate=1 ant
