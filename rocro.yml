inspecode:
  tasks:
    - name: code-analysis
      type: shell
      git-submodules:
        set-up:
          - git submodule update --init --recursive
        sharing: "yes"
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
