inspecode:
  tasks:
    - name: code-analysis
      type: shell
      git-submodules:
        set-up:
          - git submodule update --init --recursive
        sharing:
          enable: true
          branch: vendor~submod
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
