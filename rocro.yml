inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          branch: deps~implicit
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
