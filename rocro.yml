inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: false
          branch: deps~conflict
      commands:
        - cat ./report.sarif
      reports:
        - path: ./report.sarif
