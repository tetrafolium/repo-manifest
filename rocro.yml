inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing: false
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
