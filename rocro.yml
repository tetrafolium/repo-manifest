inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: false
          without:
            - some/pattern
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
