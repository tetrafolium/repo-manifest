inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing: false
      commands:
        - cat ./report.sarif
      reports:
        - path: ./report.sarif
