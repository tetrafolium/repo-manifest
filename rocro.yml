inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: false
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
