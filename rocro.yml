inspecode:
  tasks:
    - name: git-repo-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          without: sources/meta-openembedded/ant/*
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
