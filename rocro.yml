inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          branch: deps~implicit
      commands:
        - cat ./report.sarif
      reports:
        - path: ./report.sarif
