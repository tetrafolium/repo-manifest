inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: true
          branch: "a~"
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
