inspecode:
  tasks:
    - name: nanopb-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing:
          enable: true
          branch: vendor
      commands:
        - cat ./report.sarif
      reports:
        - path: ./report.sarif
