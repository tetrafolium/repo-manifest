inspecode:
  tasks:
    - name: git-repo-task
      type: shell
      git-repo:
        manifest: ./default.xml
        sharing: true
      commands:
        - ls -alt ./
      reports:
        - path: ./report.json
